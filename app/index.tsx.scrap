// App.js
import * as React from "react";
import { Stack } from "expo-router";

function App() {
	return (
		<Stack initialRouteName="(app)/onboarding">
			<Stack.Screen name="(auth)" options={{ headerShown: false }} />
			<Stack.Screen name="(app)" />
		</Stack>
	);
}

export default App;
